version: "3.8"

services: 
  mongo: 
    image: mongo:latest
    container_name: mongodb
    ports:
      - 27017:27017
    volumes:
        - mongodb-data:/data/mongodb
        - mongodb-config:/data/mongoconfig
volumes: 
  mongodb-data:
  mongodb-config:

